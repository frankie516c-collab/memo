<h3>직접 만들어보기1</h3>

문제
```
const 기호 = "■";
for( ; ; )
{
  
}
// 출력 예시 : ■■■■■■■■■■ 
```

답
```
    const 기호 = "■";
    let 값 = "";
    for ( let i = 0 ; i < 10 ; i++ )
    {
       값 = 값 + 기호;
    }
     console.log(값);
```
- `값 = 값 + 기호;`를 `값 += 기호;`로 써도 같음

<h3>직접 만들어보기2</h3>

문제
```
const 기호 = "■";
for( ; ; )
{
  
}
// 출력 예시 : 
// ■
// ■■
// ■■■
// ■■■■
// ■■■■■
// ■■■■■■
// ■■■■■■■
// ■■■■■■■■
// ■■■■■■■■■
// ■■■■■■■■■■
```

답
```
    const 기호 = "■";
    let 값 = "";
    for ( let i = 0 ; i < 10 ; i++ )
    {
       값 = 값 + 기호;
       console.log(값);
    }
```

내 오답
```
const 기호 = "■";
    let 값 = "";
    for ( let i = 0 ; i < 10 ; i++ )
    {
       console.log(기호);
       console.log("반복");
    }
    console.log(i);
```
내 실수
- `값 = 값 + 기호`를 넣지 않음
- `console.log("값")`을 넣지 않음
- `console.log("반복")`을 넣음
- `console.log("기호")`를 넣음

왜 이런 실수를 했는가?
- `값 = 값 + 기호`를 생각하지 못함
